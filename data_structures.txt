enum Commands { Disconnect, RefreshContacts, CreateConference, AddToConference, RemoveFromConference, DeleteConference, SendMessageToContact, SendMessageToConference, RefreshStorage, UploadFileToStorage, DownloadFileFromStorage, RemoveFileFromStorage }

enum ResultTypes { UpdatedContacts, CreatedConference, AddedToConference | AddedConference, RemovedFromConference, DeletedConference | DeletedConference, Message, UpdatedFiles, UploadedFileToStorage, DownloadedFileFromStorage, RemovedFile }

class ComData
{
    Commands command;
    UUID uuid;
    Object data;
}

class ResData
{
    ResultTypes resultType;
    Object data;
}

class ClientName
{
    String name;
}

Contact {
    Name;
    Uuid;
}

ContactConfPair {
    UUID confID;
    UUID contactID
}

UuidMessagePair
{
    UUID id;
    String message;
}

File
{
    String name;
    Object data;
}

UuidFileNamePair
{
    String name;
    UUID id;
}

List<Contact> contacts;

class Conference
{
    String name;
    UUID confID;
    List<UUID> contactsIDs;
}

List<Conference> conferenes;